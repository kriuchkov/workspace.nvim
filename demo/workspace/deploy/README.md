# deploy

Deployment config for the **orbit** workspace — a non-code repo, to show that a
claudespace workspace tracks more than just services.

- [`docker-compose.yml`](docker-compose.yml) — wires the services + frontend

Fleet commands (`<leader>cw…`) treat this like any other repo.
