Commit and push the current changes in this repository.

Steps:
1. Run `git status` and `git diff` (including staged) to see what changed.
2. Run `git log --oneline -5` to match the repo's commit style.
3. Write a concise conventional-commit message from the real changes.
4. Stage everything, commit, and push. Do not add a Co-Authored-By line.
