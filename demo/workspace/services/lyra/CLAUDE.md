# lyra (Go service)

Averages user scores — part of the **orbit** workspace. Shares the
`orbit/nebula` package with the other services.

- `lyra.go` — `Average` + `main`
- `lyra_test.go` — its tests
