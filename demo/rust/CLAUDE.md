# demo (Rust)

A minimal Rust binary for exploring claudespace.nvim.

- `src/main.rs` — the `greet` function, `main`, and unit tests

## Conventions
- Keep it tiny and idiomatic — this is a playground, not production.
- Prefer `cargo clippy` clean; keep the inline `#[cfg(test)]` tests passing.
